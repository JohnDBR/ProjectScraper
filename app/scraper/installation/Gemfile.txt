###Requirements...

git clone https://JohnDBR@bitbucket.org/dijkstra_team_1/schedule_bot_john.git

ruby 2.5.0

gem install mechanize -v 2.7.5
gem install watir -v 6.10.3
gem install selenium-webdriver -v 3.9.0 //Watir installs selenium-webdriver take care with the version...
gem install webdrivers -v 3.2.4

sudo apt-get install unzip //version chromedriver=2.35.528139 
wget -O /tmp/chromedriver.zip https://chromedriver.storage.googleapis.com/2.35/chromedriver_linux64.zip
sudo unzip /tmp/chromedriver.zip chromedriver -d /usr/local/bin/
sudo unzip /tmp/chromedriver.zip chromedriver -d /usr/local/share/
sudo unzip /tmp/chromedriver.zip chromedriver -d /usr/bin/
sudo chmod +x chromedriver //Go to the /usr/local/bin/ , /usr/local/share/ , /usr/bin/ and run this command...

//sudo -s if you have a permission problem... version chrome=64.0.3282.140
curl https://dl.google.com/linux/direct/google-chrome-stable_current_amd64.deb -o /chrome.deb
sudo dpkg -i /chrome.deb || apt-get install -yf
rm /chrome.deb

\\TESTING NOTE>> This should not being installed instead of the path has to be declared on the Capabilities... 
\\TESTING NOTE>> I don't know how to uninstall the .deb, and i need to do it because it is recognized as the real driver but the scraper is working fine... 

###If you are in docker then do... (NOT WORKING ON DOCKER YET!)

docker run --privileged //Your container should have this attribute...
umount /dev/shm
mount -t tmpfs -o rw,nosuid,nodev,noexec,relatime,size=512M tmpfs /dev/shm
//Some

###If you expirience problems, there are alternative solutions... 

(If you don't have wget or curl)
sudo apt-update 
sudo apt-get install wget
sudo apt-get install curl

(If you dont have this)
apt-get install libnss3 libgconf-2-4
